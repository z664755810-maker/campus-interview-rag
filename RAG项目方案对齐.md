# 校招软开面试题库 RAG 垂直问答系统 — 方案对齐（2026-08-20）

> 角色：阿栈讲师带练（讲师口吻 + 边开发边学）。本文件为阶段0前的方案锁定文档，新对话可据此直接开工。

## 一、目标与完成标准
- 上传面试题文档 → 自动切分 + 向量化
- 基于检索增强生成（RAG）作答
- 回答带**引用溯源**（可点开看出处）
- 体现真实业务场景：**鉴权 / 输入校验 / 限流**
- 非商用，定位作品集项目 #2

## 二、技术栈（已对齐）
| 层 | 选型 | 说明 |
|---|---|---|
| 后端 | **FastAPI**（Python） | 异步、自带 OpenAPI、Pydantic 校验天然契合"校验"需求 |
| 向量库 | **Chroma（嵌入式）** | 本地持久化；向量+原文+元数据一体，最贴合引用溯源 |
| Embedding | 智谱 **embedding-3**（2048维） | 中文强，与 LLM 同生态 |
| LLM | 智谱 **GLM-4**（开发期用 flash 控成本） | 需 API Key |
| 前端 | **Vue3** | 组件化，与后端 REST 清晰对接 |
| 鉴权 | **API Key 中间件** | 请求头 token 校验，非商用足够 |

## 三、核心概念（已讲解）
- **RAG** = 开卷考试：先检索相关原文，再让模型"有据可依"作答，抑制幻觉。
- **Embedding** = 给文字发"语义坐标卡"：语义相近→向量距离近，是检索的数学基础。
- **向量检索** = 在地图上找离你最近的几个点；比关键词检索更抗"换种问法"。
- ⚠️ 关键坑：Chroma 默认 embedding 不是智谱，必须**自写 embedding function 调用智谱**，否则坐标不同源、检索必翻车。

## 四、最小可跑架构
```
[Vue3 前端] ──REST──> [FastAPI 后端]
                         ├─ 鉴权+限流中间件
                         ├─ /upload 入库：解析→切分→智谱向量化→写 Chroma
                         ├─ /ask 问答：检索 top-k→拼Prompt→GLM生成→返回答案+引用
                         └─ 引用溯源：chunk 带 文件名/题号 元数据
                              └─ Prompt 标号[1][2]，返回出处映射
        [FastAPI] ──> [Chroma 向量库]（本地磁盘，向量+原文+元数据）
        [FastAPI] ──> [智谱 GLM API]（embedding-3 / GLM-4）
```
- 引用溯源实现：检索 top-k 后，Prompt 给每段标 `[1][2][3]`，要求模型标注引用序号，前端按映射展示"出处卡片"。

## 五、分阶段实施（每步可跑可验证）
- **阶段0 脚手架**：FastAPI 空服务 + Vue3 空页面，跑通 hello。
- **阶段1 入库管线**：样例 md → `/upload` 切分+向量化+入库；**按"题"切分**保住单题完整性；验证检索命中。
- **阶段2 问答管线**：`/ask` 检索+生成+引用溯源；curl 验证"带出处"回答。
- **阶段3 前端**：上传组件 + 对话组件（答案下展示引用卡片）。
- **阶段4 业务加固**：API Key 鉴权 + Pydantic 校验 + 限流；并讲生产环境考量。

## 六、前置准备
- 需准备**智谱开放平台 API Key**，建议用环境变量 `ZHIPU_API_KEY` 注入（勿硬编码进仓库）。
- 样例数据由讲师生成（多科目面试题 markdown：Java / Python / 计网 / 数据库 / 操作系统等）。

## 七、下一步
确认后从**阶段0**开始：搭建项目骨架（后端 venv + FastAPI 最小服务，前端 Vue3 初始工程）。
