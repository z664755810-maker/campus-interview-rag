# 段A 修复 + 段B 新功能（commit a821bd1）

日期：2026-08-31 ｜ 项目：校招面试题库 RAG 系统（project-2）

## 一、段A：解决用户 4 项反馈

| # | 反馈 | 修复 | 文件 |
|---|---|---|---|
| 1 | 提问框太小 | 全宽自适应 textarea（最少 3 行 / 最多 8 行，附快捷键提示） | `frontend/src/components/ChatPanel.vue` |
| 2 | 题库只能加不能删 | `DELETE /api/documents/by-source/{source}` + `POST /api/documents/clear`（需 `confirm=true` 二次确认） | `backend/app/api/documents.py` |
| 3 | API Key 输入框不知何用 | 移除前端输入框；改为 `/api/usage` 限流配额指示器（今日用量/余量），后端真实鉴权保留 | `frontend/src/App.vue` + `backend/app/api/documents.py` + `backend/app/main.py` |
| 4 | 溯源不相关 | `vector_store.search` 加 `min_score` 阈值过滤（默认 0.5，余弦距离 > 阈值丢弃）；`rag.py` prompt 加"资料不相关就明说未包含相关内容"护栏；`top_k` 默认 4→5 | `backend/app/services/vector_store.py` + `backend/app/services/rag.py` |

## 二、段B：新增求职场景功能

- **B2 专题刷题**：`GET /api/documents/by-subject` 按学科分组；`SubjectBrowser.vue` 浏览/点开看完整题/一键让 RAG 讲解
- **B3 模拟面试**：`GET /api/documents/random` 随机抽题；`InterviewMode.vue` 倒计时 + 自评（对/错/不确定）+ 评分 + 错题列表

## 三、工程修复

- `zhipu._mock_embed` 修复 NaN/Inf（hash 字节直接解 float 会产生非有限值 → Chroma 拒收），改有限性兜底 + L2 归一化。`MOCK_EMBED` 仅本地/CI 用，生产默认 false
- 新增 `GET /api/usage`（限流配额，公开路径）、`GET /api/documents/formats`（前后端白名单共用）

## 四、验证状态

- ✅ MOCK 模式端到端全通过：灌库 50 / 6 学科分组 / 随机抽题 / Word 上传 50 / 按源删除 50 / 清空 / 检索结构 / 问答结构
- ⏳ **待验证**：`min_score=0.5` 溯源阈值需在真实智谱 API 额度恢复后做一遍真实数据验证（当前 embedding 接口 429 限流）

## 五、下一步（用户操作）

1. GitHub Desktop 推送 `a821bd1` → Render 自动重建（新增 4 依赖，构建约 4-6 分钟）
2. 访问 https://campus-interview-rag.onrender.com 实测三模式（问答 / 专题刷题 / 模拟面试）
3. 真实 API 额度恢复后，验证溯源阈值是否过严/过松
